# 重新发拍功能需求文档 V1.3

| 项目 | 内容 |
|------|------|
| **版本** | V1.3 |
| **日期** | 2026-04-16 |
| **状态** | ✅ 已实现 |
| **作者** | 润喉糖 |

---

## 1. 功能概述

### 1.1 背景
经销商在发拍管理过程中，车辆可能因各种原因被下架（草稿下架、待拍卖下架）或流拍。为提高发拍效率，系统需要支持重新发拍功能，让经销商可以快速将车辆重新推入拍卖流程。

### 1.2 功能目标
1. 支持不同来源的重新发拍（草稿下架 vs 待拍卖下架）
2. 区分编辑权限（完整编辑 vs 仅价格修改）
3. 统一4步流程，保持操作一致性
4. 下架来源标记，便于业务追溯

---

## 2. 重新发拍来源与权限矩阵

| 来源状态 | 触发条件 | 编辑模式 | 可编辑字段 | 流程步数 | 提交后状态 |
|----------|----------|----------|------------|----------|------------|
| **草稿下架** (`offshelf` + `draft`) | 草稿→下架→重新发拍 | 完整编辑 | 全部字段 | 4步 | 待拍卖 |
| **待拍卖下架** (`offshelf` + `scheduled`) | 待拍卖→下架→重新发拍 | 仅价格修改 | 仅保留价 | 4步 | 待拍卖 |

> **下架来源标记**：车辆下架时自动记录 `offshelfSource`（draft/scheduled），用于区分重新发拍权限。

---

## 3. 详细需求

### 3.1 重新发拍（统一4步流程）

#### 3.1.1 触发条件
- 车辆状态为「已下架」
- 用户点击【重新发拍】按钮

#### 3.1.2 URL参数
```
/form?edit={id}&resubmit={full|price}
```

| 参数值 | 来源 | 说明 |
|--------|------|------|
| `full` | 草稿下架 | 完整编辑模式，所有字段可编辑 |
| `price` | 待拍卖下架 | 仅价格修改模式，其他字段只读 |

#### 3.1.3 页面标题
显示「重新发拍」

#### 3.1.4 表单流程（统一4步）

| 步骤 | 页面 | 包含字段 | 草稿下架 | 待拍卖下架 |
|------|------|----------|----------|------------|
| Step 1 | 基本信息 | VIN码、车牌号、品牌、车系、车型名称、年款、排量、变速箱、燃料类型、外饰颜色、内饰颜色、所在城市、车辆性质 | 可编辑 | 只读 |
| Step 2 | 车辆详情 | 表显里程、上牌日期、过户次数 | 可编辑 | 只读 |
| Step 3 | 价格设置 | 保留价 | 可编辑 | **可编辑** |
| Step 4 | 照片上传 | 车辆照片（7张） | 可编辑 | 只读（显示已有照片） |

#### 3.1.5 第4步照片展示（待拍卖下架）

```
┌─────────────────────────────┐
│ 必拍位置        7/7 已上传  │
├─────────────────────────────┤
│ [✓正前] [✓左前45°] [✓右前45°]│
│ [✓右后45°] [✓内饰] [✓仪表台] │
│ [✓发动机舱]                 │
└─────────────────────────────┘
```

- 绿色勾标记表示已上传
- 无上传/删除按钮（只读）
- 显示已有的7张照片缩略图

#### 3.1.6 表单校验

| 字段 | 校验规则 |
|------|----------|
| VIN码 | 必填，17位字符 |
| 车牌号 | 选填，格式校验 |
| 品牌/车系/车型 | 必填 |
| 排量/变速箱 | 必填 |
| 所在城市 | 必填（省份+城市二级联动） |
| 表显里程 | 必填，数字 |
| 上牌日期 | 必填 |
| 保留价 | 必填，数字（万元） |

#### 3.1.7 底部按钮
- 【上一步】：当前步骤>1时显示
- 【下一步】：步骤1-3显示
- 【确认重新发拍】：步骤4显示
- 【保存草稿】：不显示（重新发拍不保存草稿）

#### 3.1.8 提交流程
1. 点击【确认重新发拍】
2. 校验必填字段
3. 更新车辆状态为 `scheduled`（待拍卖）
4. 更新 `auctionStartTime` 为当前时间
5. 清除 `offshelfSource` 字段（重新进入拍卖流程）
6. 显示成功提示：「重新发拍成功」
7. 跳转至「待拍卖」Tab 列表页

#### 3.1.9 返回逻辑
- 点击【上一步】返回上一页
- 点击左上角返回按钮：返回列表页

---

## 4. 下架功能

### 4.1 下架操作
- 点击【下架车辆】按钮
- 弹出二次确认弹窗：「确定下架该车辆？下架后可重新发拍」
- 确认后更新状态为 `offshelf`
- 自动记录 `offshelfSource`（当前状态）

### 4.2 下架来源字段

```typescript
offshelfSource: 'draft' | 'scheduled'
```

| 值 | 说明 |
|-----|------|
| `draft` | 从草稿状态直接下架 |
| `scheduled` | 从待拍卖状态下架（已进入拍卖流程） |

---

## 5. 列表卡片展示

### 5.1 已下架卡片标签

已下架车辆在列表卡片上显示来源标签：

```
┌─────────────────────────────┐
│ FP20260409001    [已下架]  │
│ 来自待拍卖下架 ← 琥珀色标签 │
│ 奥迪 Q5L                   │
│ ...                        │
│ [重新发拍] [查看]           │
└─────────────────────────────┘
```

### 5.2 标签样式
- 颜色：琥珀色边框 `border border-amber-300`
- 背景：`bg-amber-50`
- 文字：`text-amber-700 text-[10px]`
- 内容：
  - 草稿下架：「来自草稿下架」
  - 待拍卖下架：「来自待拍卖下架」

---

## 6. 按钮池完整规则

| Status | 状态中文名 | 按钮组合 | 说明 |
|--------|------------|----------|------|
| `draft` | 草稿 | 【编辑发拍单】【下架车辆】【查看】 | 草稿可编辑、可下架 |
| `scheduled` | 待拍卖 | 【下架车辆】【查看】 | 待拍卖可下架 |
| `auctioning` | 拍卖中 | 【查看】 | 锁定业务，仅查看 |
| `sold` | 交易成功 | 【查看】 | 锁定业务，仅查看 |
| `unsold` | 流拍 | 【重新发拍】【查看】 | 可重新发拍 |
| `offshelf` | 已下架 | 【重新发拍】【查看】 | 可重新发拍 |

---

## 7. 状态流转图

```
┌─────────┐                    ┌─────────┐                    ┌─────────┐
│  草稿   │ ──提交发拍──>      │ 待拍卖  │ ──开拍──>          │ 拍卖中  │
└─────────┘                    └─────────┘                    └─────────┘
     │                              │                              │
     │ 下架                         │ 下架                         │
     ▼                              ▼                              ▼
┌─────────┐                    ┌─────────┐                    ┌─────────┐
│已下架   │                    │已下架   │                    │交易成功 │
│(draft)  │                    │(sched)  │                    └─────────┘
└─────────┘                    └─────────┘                         │
     │                              │                              │
     │                              │                              │
     │                              │                              ▼
     │                              │                         ┌─────────┐
     │                              │                         │  流拍   │
     │                              │                         └─────────┘
     ▼                              ▼                              │
┌─────────────────────────────────────────┐                      │
│           重新发拍                       │◀─────────────────────┘
│  ┌────────────────────────────────────┐ │
│  │ 草稿下架：完整编辑（4步）          │ │
│  │ 待拍卖下架：仅改保留价（4步只读）   │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
                    │
                    ▼
              ┌─────────┐
              │ 待拍卖  │
              └─────────┘
```

---

## 8. 数据模型

### 8.1 车辆状态枚举
```typescript
type VehicleStatus =
  | 'draft'       // 草稿
  | 'scheduled'   // 待拍卖
  | 'auctioning'  // 拍卖中
  | 'sold'        // 交易成功
  | 'unsold'      // 流拍
  | 'offshelf';   // 已下架
```

### 8.2 下架来源枚举
```typescript
type OffshelfSource =
  | 'draft'       // 从草稿下架
  | 'scheduled';  // 从待拍卖下架
```

### 8.3 Application 接口
```typescript
interface Application {
  id: string;
  applyNo: string;
  vin: string;
  licensePlate?: string;
  carBrand: string;
  carSeries: string;
  carModel: string;
  carYear?: number;
  engineCapacity?: string;
  transmission?: string;
  fuelType?: string;
  exteriorColor?: string;
  interiorColor?: string;
  province: string;
  city: string;
  mileage?: number;
  registrationDate?: string;
  transferCount?: number;
  vehicleNature?: string;
  reservePrice: number | null;
  images: {
    front: string;
    frontLeft: string;
    frontRight: string;
    rearRight: string;
    interior: string;
    dashboard: string;
    engine: string;
  };
  status: VehicleStatus;
  offshelfSource?: OffshelfSource;  // 下架来源
  auctionStartTime?: string;
  applyTime: string;
  submitterName: string;
  submitterPhone: string;
}
```

---

## 9. 模拟数据示例

### 9.1 草稿下架车辆
```typescript
{
  id: '8',
  applyNo: 'FP20260416008',
  vin: 'LBV3Z3C53NC876543',
  licensePlate: '沪A88888',
  carBrand: '奔驰',
  carSeries: 'GLC',
  carModel: '2023款 GLC 300 L 4MATIC 豪华型',
  carYear: 2023,
  engineCapacity: '2.0T',
  transmission: '自动',
  fuelType: '汽油',
  exteriorColor: '白色',
  interiorColor: '黑色',
  province: '上海',
  city: '上海',
  mileage: 8000,
  registrationDate: '2023-08',
  transferCount: 0,
  vehicleNature: '非营运',
  reservePrice: 42.0,
  images: {
    front: 'p1', frontLeft: 'p2', frontRight: 'p3',
    rearRight: 'p4', interior: 'p5', dashboard: 'p6', engine: 'p7'
  },
  status: 'offshelf',
  offshelfSource: 'draft',  // 来自草稿下架
  applyTime: '2026-04-16 14:00',
  submitterName: '张三',
  submitterPhone: '138****1234'
}
```

### 9.2 待拍卖下架车辆
```typescript
{
  id: '6',
  applyNo: 'FP20260409006',
  vin: 'LAV3Z3C52NC345678',
  licensePlate: '沪A66666',
  carBrand: '奥迪',
  carSeries: 'Q5L',
  carModel: '2023款 45 TFSI 豪华动感型',
  carYear: 2023,
  engineCapacity: '2.0T',
  transmission: '自动',
  fuelType: '汽油',
  exteriorColor: '黑色',
  interiorColor: '棕色',
  province: '北京',
  city: '北京',
  mileage: 15000,
  registrationDate: '2023-06',
  transferCount: 1,
  vehicleNature: '非营运',
  reservePrice: 38.0,
  images: {
    front: 'p1', frontLeft: 'p2', frontRight: 'p3',
    rearRight: 'p4', interior: 'p5', dashboard: 'p6', engine: 'p7'
  },
  status: 'offshelf',
  offshelfSource: 'scheduled',  // 来自待拍卖下架
  auctionStartTime: '2026-04-10 14:00',
  applyTime: '2026-04-09 09:30',
  submitterName: '王五',
  submitterPhone: '139****5678'
}
```

---

## 10. 测试用例

### 10.1 草稿下架重新发拍
- [ ] 草稿状态显示【下架车辆】按钮
- [ ] 点击下架，弹出确认弹窗
- [ ] 确认后状态变为「已下架」
- [ ] 卡片显示「来自草稿下架」标签
- [ ] 点击【重新发拍】，跳转编辑页面
- [ ] 4步流程，所有字段可编辑
- [ ] 提交后状态变为「待拍卖」
- [ ] 跳转至待拍卖 Tab

### 10.2 待拍卖下架重新发拍
- [ ] 待拍卖状态显示【下架车辆】按钮
- [ ] 点击下架，弹出确认弹窗
- [ ] 确认后状态变为「已下架」
- [ ] 卡片显示「来自待拍卖下架」标签
- [ ] 点击【重新发拍】，跳转编辑页面
- [ ] 4步流程，仅保留价可编辑
- [ ] 第4步显示已有7张照片（只读）
- [ ] 提交后状态变为「待拍卖」
- [ ] 跳转至待拍卖 Tab

### 10.3 流拍重新发拍
- [ ] 流拍状态显示【重新发拍】按钮
- [ ] 点击后跳转编辑页面
- [ ] 4步流程，所有字段可编辑
- [ ] 提交后状态变为「待拍卖」

---

## 11. 修订历史

| 版本 | 日期 | 修订内容 | 作者 |
|------|------|----------|------|
| V1.0 | 2026-04-16 | 初始版本 | 润喉糖 |
| V1.1 | 2026-04-16 | 调整重新发拍流程：所有来源统一使用完整4步流程，移除仅改保留价模式 | 润喉糖 |
| V1.2 | 2026-04-16 | 重新区分来源：草稿下架完整编辑4步，待拍卖下架仅改保留价；增加下架来源标签显示 | 润喉糖 |
| V1.3 | 2026-04-16 | 统一4步流程，待拍卖下架仅保留价可编辑，其他只读；第4步显示已有照片 | 润喉糖 |

---

*本文档最终解释权归发拍管理系统开发团队所有*
