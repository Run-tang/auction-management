# 优加发拍 - 经销商端需求文档

> **文档版本**: V1.0
> **最后更新**: 2026-04-14
> **产品名称**: 优加发拍（经销商端）
> **适用对象**: 二手车经销商、内外部发拍者

---

## 一、全局业务逻辑与规范补充声明（前置共识）

### 1.1 数据解耦与导出规范
发拍端呈现在界面的组合型数据（如列表的"排量/变速箱"），在前端表单提交和后端数据库存储时，必须严格拆解为独立字段（Key-Value），严禁在库内拼接，确保后台管理系统能"一列展示一个字段"完美导出 Excel。

### 1.2 状态流转按钮权限的补充
针对不同状态的卡片，控制其唯一合法的操作按钮。例如"重新发拍"其底层逻辑是：一键复制原单据的所有数据，进入【新增发拍】第一步，减少车商重复录入成本。

### 1.3 提交直接通过
经销商提交发拍申请后，直接进入"待发拍"状态，无需后台审核。提交成功后，后台通过 Hook 协议推送企微消息通知。

### 1.4 排序规则
发拍管理列表按照创建时间倒序排序。

---

## 二、登录与个人中心

### 2.1 登录与身份鉴权

| 模块 | 功能说明 |
|------|----------|
| **登录账号** | 默认为手机号，支持 11 位长度输入限制 |
| **验证密码** | 平台后台派发，长度限制 6-16 位字母与数字 |

#### 关联业务逻辑

| 规则 | 说明 |
|------|------|
| **白名单准入核验** | 用户点击登录后，接口先向后台校验手机号是否存在于"发拍白名单"库中，未建档者不允许登入 |
| **Token 过期管理** | 成功登录下发长效 Token 机制（30 天免密登入），若后台将该账号标记为"拉黑/离职注销"，当前 Token 立即失效强制踢出 |

#### 交互说明

| 场景 | 说明 |
|------|------|
| **一键授权登录** | 支持微信端拉起屏幕底部授权弹窗，点击"允许"一键换取手机号登入 |
| **常态键入** | 拉起原生数字小键盘。输入账号后，"登录"主按键由置灰变为高亮可点击 |
| **异常阻断 Toast** | 若账号未在系统注册，前端阻断并用弱提示（Toast）居中报错："仅限内部员工使用，请前往申请" |

#### 数据字典

| 字段 | 类型 | 必填 | 限制说明 |
|------|------|------|----------|
| 手机号 | String | 是 | 11 位数字，以 1 开头 |
| 密码 | String | 是 | 6-16 位字母与数字组合 |

---

### 2.2 我的模块（个人中心）

| 模块 | 功能说明 |
|------|----------|
| **个人档案卡片** | 展示【真实姓名】、【手机号】、【业务账号ID】（纯数字） |
| **动态统计看板** | - 【发拍总数】：取历史至今所有创建并成功提交累加的数据集<br>- 【成交数量】：由后台成功撮合且状态流转至"已成交"的总和<br>- 【成交率】：派生公式计算（成交数量 / 发拍总数 * 100%） |

#### 交互说明

| 场景 | 说明 |
|------|------|
| **检测管理入口** | 点击"检测管理"区域，拉起端内 WebView 视窗全屏，展示关联检测后台的网页。此 Web 应用免密接入门槛：由 APP 首开携带长期（30 天活跃）登录 Cookie 实现穿透 |
| **退出登录** | 点击"退出登录"，回退至入口登录页 |

#### 数据字典

| 字段 | 类型 | 说明 |
|------|------|------|
| 真实姓名 | String | 后台配置的经销商名称 |
| 手机号 | String | 11 位脱敏展示（如 138****8000） |
| 账号ID | String | 纯数字，如 D001 |
| 发拍总数 | Number | 历史累计 |
| 成交数量 | Number | 状态为"交易成功"的数量 |
| 成交率 | Number | 成交数量/发拍总数 * 100%，保留整数 |

---

## 三、发拍管理列表

### 3.1 功能说明
集中呈现车辆生命周期状态，提供依据状态分流的操作入口。

### 3.2 字段说明（卡片展示级）

#### 顶部状态页签（Tab）

| Tab 顺序 | 说明 |
|----------|------|
| 全部 | 展示所有发拍申请 |
| 草稿 | 处于草稿状态 |
| 待发拍 | 提交后待发布 |
| 待拍卖 | 已排期等待拍卖 |
| 拍卖中 | 正在进行拍卖 |
| 交易成功 | 已成交 |
| 流拍 | 拍卖未成交 |
| 已下架 | 已下架冻结 |

#### 摘要字段

| 字段 | 说明 | 示例 |
|------|------|------|
| 车型名称 | 长文本截断显示 | 宝马 5系 |
| 车牌号 | 最多 8 位 | 沪A12345 |
| 排量/变速箱 | 前端拼接展示 | 2.0T/自动 |
| 保留价 | 加￥符号显示 | ¥36.0万 |
| 里程 | 数值 + 万公里 | 3.2万公里 |
| 申请时间 | 截取月-日显示 | 03-31 |

#### 后端解耦字段映射（重要）
卡片上显示的"排量/变速箱"，由后端下发的独立【排量】和【变速箱】两段字段在前端经"/"斜杠拼接渲染。

### 3.3 操作按钮权限

根据车辆流转的 Status，映射按钮池（数组集合）：

| 状态 | 操作按钮 |
|------|----------|
| **草稿** | 【编辑发拍单】、【下架车辆】 |
| **待发拍** | 【下架车辆】 |
| **待拍卖 / 拍卖中** | 无（锁定业务） |
| **交易成功** | 无 |
| **流拍 / 已下架** | 【重新发拍】（主按钮突显） |

#### 交互说明

| 操作 | 说明 |
|------|------|
| **编辑发拍单** | 点击后，跳转至分布式表单，并且将之前的历史录入数据、已上传的照片缓存进行全屏回显（反填至输入框），方便用户在此基础上继续增删 |
| **重新发拍** | 这是整个系统最提效的魔法按钮。点击时，触发底层 Deep Copy 机制，将这台老车的车架号、上牌日期、图片等全量提取出来，生成一条"全新草稿"直接送入【新增第1步】。此时业务员往往只需微调"保留价"或更新"表显里程"即可一键再次抛向市场，极速激活流拍资产 |
| **下架车辆** | 属于不可逆的高危中断操作。点击后必须触发底部原生 ActionSheet 面板或二次确认弹窗："您正在下架该发拍申请，下架后数据将冻结，是否确认？"确认后，请求接口抽离该记录 |

### 3.4 列表交互规范

| 场景 | 说明 |
|------|------|
| **初始化与防白屏** | 首次进入页面或切换 Tab 时，在接口未返回前，列表区展示骨架屏 |
| **下拉刷新** | 在列表顶部继续向下拉动，手势释放后触发局部接口刷新，重置 pageNo=1，获取最新数据并收敛刷新动画 |
| **上拉加载更多（触底分页）** | 滑动到距离屏幕底部剩余 20% 时，静默触发下一页数据请求叠加。若后端返回数据包<20条，列表底部追加提示文本："已展示全部申请"，并锁定上拉事件 |
| **全局发拍入口** | 页面右下角无论怎么滑动，常驻悬浮一个醒目的【➕】主色调按钮。点击直接进入【新增发拍-第1步】 |
| **Tab 切换防抖** | 快速点击不同 Tab 时，必须做 300ms 的请求防抖（Debounce）处理，废弃未完成的旧接口响应，防止不同 Tab 下的数据相互串门渲染 |

---

## 四、新增发拍申请

### 4.1 步骤概览

| 步骤 | 名称 | 说明 |
|------|------|------|
| 1/4 | 基础信息 | 车辆识别 + 品牌车型 + 车辆配置 |
| 2/4 | 车辆详情 | 登记信息（上牌日期、表显里程、过户次数、车辆性质） |
| 3/4 | 价格设置 | 拍卖参数（保留价） |
| 4/4 | 照片上传 | 必拍位置（7张）+ 补充照片 |

### 4.2 第一步：基础信息

#### 车辆识别

| 字段 | 必填 | 限制说明 |
|------|------|----------|
| VIN车架号 | 是 | 强制校验录入长度必须为 17 位且仅限英文字母与数字。PlaceHolder 提示："请输入17位VIN码" |
| 车牌号 | 否 | 需支持输入7位（蓝黄牌）或8位（新能源绿牌）长度，防错校验剔除非法字符 |

#### 品牌车型（后台级联规范）

点击触发"省市区"式的级联底部弹窗（Picker），必须依次获取：【品牌】→【车系】→【车型名称】→【年款】

| 字段 | 必填 | 说明 |
|------|------|------|
| 品牌 | 是 | 唤起品牌选择器 |
| 车系 | 是 | 唤起车系选择器（根据品牌级联） |
| 车型名称 | 是 | 选择对应的车型 |
| 年款 | 是 | 选择车辆的年款信息 |

#### 车辆配置

| 字段 | 必填 | 限制说明 |
|------|------|----------|
| 排量 | 是 | 最大输入长度限制10位字符 |
| 变速箱类型 | 否 | 单选枚举：手动、自动 |
| 燃料类型 | 否 | 单选枚举：汽油、柴油、电动、混动 |
| 车身颜色 | 否 | 调起标准色板菜单（黑、白、银、灰、红、蓝、绿、棕），供单选 |
| 内饰颜色 | 否 | 同车身颜色 |

### 4.3 第二步：车辆详情

#### 登记信息

| 字段 | 必填 | 限制说明 |
|------|------|----------|
| 上牌日期 | 是 | 唤起原生 Date-Picker 日期滚轮，格式需精确至"YYYY-MM" |
| 表显里程 | 是 | 数值键盘限制：最长5位整数（即99999），最多允许输入2位小数。框外右侧常驻静态单位"万公里" |
| 过户次数 | 否 | 数字键盘强限制：只允许输入1位阿拉伯数字（0-9之间），框外右侧常驻单位"次" |
| 车辆性质 | 否 | 底部单选菜单：非营运、营运 |

### 4.4 第三步：价格设置

#### 拍卖参数

| 字段 | 必填 | 限制说明 |
|------|------|----------|
| 保留价 | 是 | 核心资产字段。调起带小数点的数字键盘，限制：最长7位数，最多2位小数。单位为"万元" |

#### 提示词（强显）
在输入框下方使用异色（如浅红或灰色）小号字常驻展示：
> ⚠️ 保留价对买家不可见，低于保留价不成交

### 4.5 第四步：照片上传

#### 必拍位置（设为硬指标）

包含 7 个锚位，每个锚位展示带有对应部位水印的虚线空相框：

| 位置 | 说明 |
|------|------|
| 前脸 | 必填 |
| 左前45° | 必填 |
| 右后45° | 必填 |
| 内饰 | 必填 |
| 仪表盘 | 必填 |
| 发动机舱 | 必填 |
| 后备箱 | 必填 |

> 说明：这 7 个图位全是必填，缺失任何一张均不允许提交。

#### 补充照片（非必填特权）

| 分类 | 说明 |
|------|------|
| 瑕疵照片 | 可选，每类最多5张 |
| 改装照片 | 可选，每类最多5张 |
| 其他 | 可选，每类最多5张 |

### 4.6 交互说明（全局链路收口）

| 场景 | 说明 |
|------|------|
| **顶部进度条** | 展示四段式进度条（Steps），当前处于第一步，完成则打√ |
| **下一步校验** | 当完成一个页面后，点击下一步必须当前页都填写完成，否则 Toast 报错 |
| **上一步返回** | 点击"上一步"可返回修改，先前填写的数据必须保留在表单上（本地状态管理机制），不可被意外清空 |
| **提交审核** | 点击"提交审核"后，系统全局遍历四个步骤必填检验 |
| **提交成功** | 若通过，前端弹出全局半透明模态框 Toast："提交成功"。并将 APP 路由引导返回至【待发拍】列表 |
| **企微通知** | 异步触达动作：后台接口在判定接收数据成功入库后，自动通过 Hook 协议推送到【企业微信后台群】 |
| **草稿防护** | 如果用户在此处意外杀掉进程或左划直接退出应用，系统会在本地持久化缓存全套填写历史；下次进发拍只要点回这条数据的草稿，可直接从中断步恢复 |
| **保存草稿** | 点击"保存草稿"按钮，将当前数据保存为草稿状态，进入草稿列表 |

---

## 五、申请详情（查看态）

### 5.1 功能说明
提供数据入库后供复用、核对的静态全局视野。

### 5.2 页面结构

#### Tab 页签
- 基础信息
- 车辆详情
- 价格设置
- 照片

#### 基础信息（只读展示）

| 字段 | 说明 |
|------|------|
| 申请单号 | 系统生成的单号 |
| 车牌号 | - |
| VIN码 | 17位车架号 |
| 品牌 | - |
| 车系 | - |
| 车型 | - |
| 年款 | - |
| 排量 | - |
| 变速箱 | - |
| 燃料类型 | - |
| 车身颜色 | - |
| 内饰颜色 | - |
| 申请时间 | - |

#### 车辆详情（只读展示）

| 字段 | 说明 |
|------|------|
| 上牌日期 | YYYY-MM |
| 表显里程 | X万公里 |
| 过户次数 | X次 |
| 车辆性质 | 营运/非营运 |

#### 价格设置（只读展示）

| 字段 | 说明 |
|------|------|
| 保留价 | 保留两位小数显示"XX万元" |
| 提示 | 保留价对买家不可见，低于保留价不成交 |

#### 照片（只读展示）
展示已上传的照片网格，每张照片显示部位水印和已上传标记。

### 5.3 顶部状态标签
详情页顶部悬浮该车辆当前"生命标签（如：待发拍/流拍）"，使用状态对应的颜色样式。

### 5.4 底部操作按钮
根据当前申请状态，显示对应的操作按钮：

| 状态 | 操作按钮 |
|------|----------|
| 草稿 | 【下架车辆】、【编辑发拍单】 |
| 待发拍 | 【下架车辆】 |
| 待拍卖 | 无 |
| 拍卖中 | 无 |
| 交易成功 | 无 |
| 流拍 | 【重新发拍】 |
| 已下架 | 【重新发拍】 |

---

## 六、数据字典

### 6.1 车辆状态枚举

| 状态 | Key | 说明 |
|------|-----|------|
| 草稿 | draft | 用户保存但未提交 |
| 待发拍 | ready | 已提交，等待发布 |
| 待拍卖 | scheduled | 已排期等待拍卖 |
| 拍卖中 | auctioning | 拍卖进行中 |
| 交易成功 | sold | 已成交 |
| 流拍 | unsold | 拍卖未成交 |
| 已下架 | offshelf | 已下架冻结 |

### 6.2 燃料类型枚举

| Key | 说明 |
|-----|------|
| 汽油 | 汽油车 |
| 柴油 | 柴油车 |
| 电动 | 纯电动车 |
| 混动 | 混合动力车 |

### 6.3 车辆性质枚举

| Key | 说明 |
|-----|------|
| 营运 | 营运车辆 |
| 非营运 | 非营运车辆 |

### 6.4 变速箱类型枚举

| Key | 说明 |
|-----|------|
| 手动 | 手动挡 |
| 自动 | 自动挡 |

### 6.5 颜色枚举

| Key | 说明 |
|-----|------|
| 黑色 | - |
| 白色 | - |
| 银色 | - |
| 灰色 | - |
| 红色 | - |
| 蓝色 | - |
| 绿色 | - |
| 棕色 | - |

---

## 七、异常场景处理

| 场景 | 处理方式 |
|------|----------|
| 账号未注册 | Toast 提示："仅限内部员工使用，请前往申请" |
| 登录密码错误 | Toast 提示："密码错误" |
| VIN 格式错误 | Toast 提示："VIN码必须为17位" |
| 必填项未填写 | Toast 提示对应字段名 |
| 必拍照片不足 | Toast 提示："请至少上传6张必拍照片" |
| 下架确认 | 弹出确认弹窗，提示："您正在下架该发拍申请，下架后数据将冻结，是否确认？" |
| Token 失效 | 强制跳转登录页 |
| 账号被拉黑 | 强制踢出并提示 |

---

## 八、历史版本

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-04-14 | 初始版本，基于需求文档生成 |

---

*文档结束*