# 需求文档

**项目名称：** 广联二手车拍卖平台  
**文档名称：** 发拍管理后台系统管理模块  
**文档编号：** PRD-GL-004  
**版本号：** V1.0  
**创建日期：** 2026-04-16  
**文档状态：** 已评审  
**作者：** 产品设计组  

---

## 文档修订记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| V1.0 | 2026-04-16 | 初稿创建，反向整理系统管理模块需求 | 产品设计组 |

---

## 目录

1. [需求背景](#需求背景)
2. [需求概述](#需求概述)
3. [功能模块一：系统用户管理](#功能模块一系统用户管理)
   - 3.1 系统用户列表
   - 3.2 新建系统用户
   - 3.3 用户详情查看
   - 3.4 用户状态启用/禁用
   - 3.5 重置用户密码
4. [功能模块二：账号列表管理](#功能模块二账号列表管理)
   - 4.1 经销商账号列表
   - 4.2 新增经销商账号
   - 4.3 账号启用与停用
   - 4.4 重置账号密码
5. [功能模块三：操作日志](#功能模块三操作日志)
   - 5.1 日志列表展示
   - 5.2 日志筛选与查询
6. [非功能需求](#非功能需求)
7. [数据字典](#数据字典)
8. [验收标准](#验收标准)
9. [相关依赖与风险](#相关依赖与风险)

---

## 需求背景

### 业务背景

广联二手车拍卖平台发拍管理后台（以下简称"后台"）承担以下核心业务：

1. **发拍审核**：后台运营人员对经销商提交的发拍申请进行审核
2. **账号管理**：管理后台内部系统用户，以及经销商账号的创建与管控
3. **操作审计**：记录后台所有用户行为，满足审计要求

随着平台使用规模扩大，后台系统管理模块需要具备完整的用户角色体系、账号生命周期管理以及操作行为审计能力。

### 问题与痛点

**系统用户管理缺失：**

1. 后台缺少内部操作人员的角色体系，无法区分超级管理员、系统管理员、运营人员等不同权限级别
2. 系统用户（后台操作员）与经销商账号混用管理，职责边界不清晰
3. 离职或违规操作员无法快速禁用账号，存在安全隐患

**经销商账号管控不足：**

1. 新增经销商账号时无初始密码机制，账号交付流程不规范
2. 经销商账号无停用/启用功能，违规经销商无法及时封禁
3. 遗忘密码的经销商无自助找回通道，需人工介入重置，效率低

**操作审计缺失：**

1. 后台操作无日志记录，出现问题时无法追溯操作路径
2. 敏感操作（禁用账号、审核发拍、创建拍卖等）缺乏留存记录

### 目标

1. 建立清晰的后台内部用户角色体系，实现差异化权限管控
2. 规范经销商账号的全生命周期管理（创建→使用→禁用→重置）
3. 建立操作日志机制，满足行为审计与问题排查需求

---

## 需求概述

| 编号 | 功能模块 | 功能名称 | 优先级 | 影响端 |
|------|----------|----------|--------|--------|
| F001 | 系统用户管理 | 系统用户列表 | P0 | 发拍管理后台 |
| F002 | 系统用户管理 | 新建系统用户 | P0 | 发拍管理后台 |
| F003 | 系统用户管理 | 用户详情查看 | P1 | 发拍管理后台 |
| F004 | 系统用户管理 | 用户状态启用/禁用 | P0 | 发拍管理后台 |
| F005 | 系统用户管理 | 重置用户密码 | P1 | 发拍管理后台 |
| F006 | 账号列表管理 | 经销商账号列表 | P0 | 发拍管理后台 |
| F007 | 账号列表管理 | 新增经销商账号 | P0 | 发拍管理后台 |
| F008 | 账号列表管理 | 账号启用与停用 | P0 | 发拍管理后台 |
| F009 | 账号列表管理 | 重置账号密码 | P1 | 发拍管理后台 |
| F010 | 操作日志 | 日志列表展示 | P1 | 发拍管理后台 |
| F011 | 操作日志 | 日志筛选与查询 | P2 | 发拍管理后台 |

---

## 功能模块一：系统用户管理

> **模块说明：** 系统用户指发拍管理后台的内部操作人员（如超级管理员、系统管理员、运营人员、只读用户）。与经销商账号相互独立，管理入口分开。

### 3.1 系统用户列表

**功能编号：** F001

**功能描述：**  
展示后台所有内部操作用户，支持按姓名、用户名、手机号搜索，按角色筛选，并以角色统计卡片的形式在列表上方概览各角色人数。

**页面布局：**

```
[统计卡片区]  超级管理员 N 人 | 系统管理员 N 人 | 运营人员 N 人 | 只读用户 N 人
[筛选区]     搜索框（姓名/用户名/手机）+ 角色筛选下拉 + 重置按钮
[列表区]     表格（用户信息 | 部门 | 联系方式 | 角色 | 最近登录 | 状态 | 创建时间 | 操作）
[底部]       共 N 个用户
```

**列表字段说明：**

| 字段 | 说明 |
|------|------|
| 用户信息 | 显示头像首字 + 真实姓名 + 用户名（等宽字体） |
| 部门 | 所属部门名称 |
| 联系方式 | 手机号 + 邮箱（两行） |
| 角色 | 角色标签（带颜色区分） |
| 最近登录 | 最后登录时间；从未登录显示"从未" |
| 状态 | Switch 开关 + 文字（启用/禁用） |
| 创建时间 | 账号创建日期 |
| 操作 | 详情按钮 + 重置密码按钮 |

**角色颜色规范：**

| 角色 | 标签颜色 |
|------|----------|
| 超级管理员 | 红底红字（#FEE2E2 / #B91C1C） |
| 系统管理员 | 紫底紫字（#EDE9FE / #6D28D9） |
| 运营人员 | 蓝底蓝字（#DBEAFE / #1D4ED8） |
| 只读用户 | 灰底灰字（#F1F5F9 / #475569） |

**交互规则：**

1. 列表默认按创建时间倒序排列
2. 搜索支持姓名、用户名、手机号的模糊匹配，实时过滤
3. 角色筛选支持选择单一角色或"全部角色"
4. 超级管理员账号的状态 Switch 为禁用态，不可操作

---

### 3.2 新建系统用户

**功能编号：** F002

**功能描述：**  
通过弹窗表单新建后台系统用户，填写基本信息并分配角色。

**触发方式：** 点击列表页右上角「新建用户」按钮

**弹窗表单字段：**

| 字段 | 类型 | 是否必填 | 校验规则 |
|------|------|----------|----------|
| 登录账号 | 文本输入 | 必填 | 不可为空 |
| 真实姓名 | 文本输入 | 必填 | 不可为空 |
| 手机号 | 文本输入 | 必填 | 11位有效手机号 |
| 邮箱 | 文本输入 | 选填 | 邮箱格式 |
| 部门 | 文本输入 | 选填 | - |
| 角色 | 下拉选择 | 必填 | 超级管理员不可在此处选择 |

**初始密码规则：**  
系统默认初始密码为用户手机号后6位，创建后需通知用户首次登录时修改密码。

**按钮状态：**  
「创建」按钮在登录账号、真实姓名、手机号任一为空时保持禁用态。

**创建成功：**  
关闭弹窗，列表刷新，新用户出现在列表首位。

---

### 3.3 用户详情查看

**功能编号：** F003

**功能描述：**  
以弹窗形式展示系统用户的全部基本信息，仅供查看，不支持直接编辑。

**展示内容：**

| 字段 | 说明 |
|------|------|
| 用户名 | 登录账号，等宽字体展示 |
| 姓名 | 真实姓名 |
| 角色 | 角色标签 |
| 部门 | 所属部门 |
| 手机号 | 联系手机 |
| 邮箱 | 联系邮箱 |
| 最近登录 | 最后登录时间 |
| 创建时间 | 账号创建日期 |

---

### 3.4 用户状态启用/禁用

**功能编号：** F004

**功能描述：**  
通过列表中的 Switch 开关切换用户状态。禁用后用户无法登录系统；启用后恢复正常访问。

**交互规则：**

1. 点击 Switch 立即切换状态，无需二次确认弹窗
2. 超级管理员账号的 Switch 置为禁用态，不可操作，防止误操作锁死系统
3. 状态切换应即时生效（前端模拟立即更新）
4. 状态文字与 Switch 联动：开启→"启用"（绿色）；关闭→"禁用"（灰色）

**异常提示：**

- 尝试禁用超级管理员时：Switch 不可点击，Tooltip 提示"超级管理员不可禁用"

---

### 3.5 重置用户密码

**功能编号：** F005

**功能描述：**  
操作员可在操作栏点击「重置」按钮，将目标用户的密码重置为默认密码（手机号后6位）。

**交互流程：**

1. 点击「重置」按钮
2. 触发 Toast 提示："密码已重置为 XXXXXX（手机号后6位）"，提供「复制」按钮
3. 操作员将新密码通知用户本人

**安全要求：**

- 重置操作需记录到操作日志
- 超级管理员的密码不允许被其他角色重置

---

## 功能模块二：账号列表管理

> **模块说明：** 账号列表管理面向经销商账号（集团账号、门店账号、系统管理员账号），是经销商使用小程序端的前提，负责账号的创建、查询、状态管控与密码管理。

### 4.1 经销商账号列表

**功能编号：** F006

**功能描述：**  
展示所有经销商账号，支持多维度筛选，分页加载，提供账号状态概览统计。

**页面布局：**

```
[筛选区]  手机号 | 姓名 | 账号类型 | 所属集团 | 重置按钮
[操作区]  共 N 条记录 | 正常 N / 停用 N                         [新增账号]
[列表区]  表格（详见字段说明）
[分页区]  每页条数选择 | 共 N 条 | 翻页控件
```

**列表字段（共10列）：**

| 列名 | 说明 |
|------|------|
| 账号ID | 唯一标识，等宽字体蓝色展示 |
| 手机号码 | 登录手机号 |
| 用户姓名 | 真实姓名 |
| 账号类型 | 系统管理员/集团账号/门店账号，带颜色标签 |
| 所属集团 | 集团名称，过长时 Tooltip 展示全名 |
| 所属门店 | 门店名称，过长时 Tooltip 展示全名；系统管理员/集团账号显示"-" |
| 账号状态 | 正常（绿色）/ 停用（灰色）标签 |
| 创建时间 | 账号创建时间 |
| 最近登录时间 | 最近一次登录时间 |
| 操作 | 启用/停用切换 + 重置密码 |

**账号类型颜色规范：**

| 类型 | 颜色 |
|------|------|
| 系统管理员 | 紫底紫字 |
| 集团账号 | 蓝底蓝字 |
| 门店账号 | 绿底绿字 |

**筛选规则：**

- 手机号：精确匹配（contains）
- 姓名：模糊匹配（contains）
- 账号类型：枚举单选，默认"全部"
- 所属集团：模糊匹配（contains）

**分页规则：**

- 每页显示条数可选：20 / 50 / 100
- 默认每页 20 条
- 筛选条件变化时重置到第1页

---

### 4.2 新增经销商账号

**功能编号：** F007

**功能描述：**  
通过弹窗表单新增经销商账号，支持系统管理员、集团账号、门店账号三种类型，根据账号类型动态展示关联字段，并自动生成默认密码。

**弹窗表单字段：**

| 字段 | 类型 | 必填 | 联动规则 |
|------|------|------|----------|
| 手机号码 | 文本输入 | 必填 | 输入满6位后自动填充默认密码 |
| 用户姓名 | 文本输入 | 必填 | - |
| 账号类型 | 下拉选择 | 必填 | 控制后续字段显示 |
| 所属集团 | 下拉选择 | 集团/门店必填 | 账号类型≠系统管理员时显示 |
| 所属门店 | 下拉选择 | 门店必填 | 账号类型=门店时显示；依赖集团选择 |
| 登录密码 | 密码输入 | 选填 | 默认值为手机号后6位，可查看/修改 |

**字段校验规则：**

| 字段 | 校验规则 | 错误提示 |
|------|----------|----------|
| 手机号码 | 11位有效手机号（1开头，第2位3-9） | "手机号格式不正确" |
| 用户姓名 | 不为空，最少2个字符 | "姓名至少2个字符" |
| 所属集团 | 非系统管理员账号必须选择 | "请选择所属集团" |
| 所属门店 | 门店账号必须选择 | "请选择所属门店" |

**密码自动填充逻辑：**

1. 用户输入手机号，当手机号长度 ≥ 6 位时，系统自动将密码字段填充为手机号后6位
2. 密码字段展示 placeholder："默认：XXXXXX"
3. 用户可手动修改密码，修改后不再跟随手机号变动
4. 密码字段旁有蓝色提示框："默认密码为手机号后6位，用户首次登录后需修改"

**联动规则：**

- 账号类型切换为"系统管理员"时：隐藏"所属集团"和"所属门店"字段
- 账号类型切换为"集团账号"时：显示"所属集团"，隐藏"所属门店"
- 账号类型切换为"门店账号"时：显示"所属集团"和"所属门店"
- 切换集团时，"所属门店"自动清空并更新门店列表

**表单校验时机：**

- 实时校验：手机号实时校验格式，输入框右侧展示成功（✓）或错误（✗）图标
- 提交校验：点击「创建账号」按钮时触发全量校验，存在错误时弹出 Toast 提示并阻止提交

**提交成功：**

- 关闭弹窗，列表刷新，Toast 提示"账号创建成功 · 账号 XX（手机号）已创建"

---

### 4.3 账号启用与停用

**功能编号：** F008

**功能描述：**  
对经销商账号进行启用或停用操作，停用后经销商无法使用该账号登录小程序端。操作需二次确认。

**交互流程：**

1. 点击操作列的启用/停用按钮（图标按钮，Tooltip 说明操作内容）
2. 弹出二次确认弹窗：
   - **停用操作**：弹窗标题"确认停用账号"，展示操作对象姓名，提示"停用后该账号将无法登录系统"（橙色警告背景）
   - **启用操作**：弹窗标题"确认启用账号"，展示操作对象姓名，提示"启用后该账号将恢复正常使用"（绿色提示背景）
3. 点击「确认停用 / 确认启用」按钮完成操作
4. 列表状态实时更新，Toast 提示操作结果

**操作按钮视觉规范：**

| 当前状态 | 操作按钮颜色 | Tooltip 文案 |
|----------|-------------|--------------|
| 正常（active） | 橙色 | 停用账号 |
| 停用（inactive） | 绿色 | 启用账号 |

**业务规则：**

- 停用操作即时生效，被停用账号的当前在线 Session 可以继续，但下次需登录时将被拒绝（当前阶段前端模拟）
- 状态变更需记录到操作日志

---

### 4.4 重置账号密码

**功能编号：** F009

**功能描述：**  
管理员可将经销商账号的登录密码重置为默认密码（手机号后6位），用于经销商忘记密码时的人工处理场景。

> **说明：** 经销商小程序端不提供自助"忘记密码"功能，所有密码重置均需联系后台管理员操作。后台也已移除"忘记密码"入口，管理员如需修改密码应使用"修改密码"功能页，密码遗忘时由超级管理员处理。

**交互流程：**

1. 点击操作列「重置密码」图标按钮
2. 立即执行重置（无需二次确认弹窗），将密码重置为手机号后6位
3. Toast 提示："密码已重置 · 新密码：XXXXXX（手机号后6位）"，提供「复制」操作
4. 点击「复制」将新密码复制到剪贴板，Toast 提示"已复制到剪贴板"

**业务规则：**

- 重置后密码为手机号后6位
- 操作记录需写入操作日志
- 管理员应通过线下方式（电话/微信）将新密码告知经销商

---

## 功能模块三：操作日志

> **模块说明：** 记录后台所有关键操作行为，支持审计追溯。日志为只读，不可删除或修改。

### 5.1 日志列表展示

**功能编号：** F010

**功能描述：**  
以表格形式展示后台操作日志，记录操作人、操作行为、操作对象、IP地址、时间及结果。

**列表字段：**

| 字段 | 说明 |
|------|------|
| 操作人 | 执行操作的系统用户真实姓名 |
| 操作模块 | 所属功能模块标签（蓝色背景） |
| 操作行为 | 操作动作描述（如"审核通过"、"新增用户"、"禁用账号"） |
| 操作对象 | 被操作的数据 ID 或标识（等宽字体） |
| IP 地址 | 操作来源 IP（等宽字体） |
| 操作时间 | 精确到秒的操作时间戳 |
| 结果 | 成功（绿色）/ 失败（红色）标签 |

**需要记录日志的操作类型：**

| 模块 | 操作行为 |
|------|----------|
| 发拍申请 | 审核通过、拒绝申请、重新审核 |
| 系统用户 | 新增用户、禁用账号、重置密码、更新角色权限 |
| 经销商账号 | 新增账号、停用账号、启用账号、重置密码 |
| 拍卖订单 | 创建拍卖、导出数据 |
| 鉴权 | 登录系统、退出登录 |

**底部展示：** "共 N 条记录"

---

### 5.2 日志筛选与查询

**功能编号：** F011

**功能描述：**  
支持按操作人/模块关键词搜索，按操作结果（全部/成功/失败）筛选。

**筛选区控件：**

| 控件 | 类型 | 说明 |
|------|------|------|
| 搜索框 | 文本输入 | 搜索操作人姓名或操作模块，实时过滤 |
| 结果筛选 | 下拉选择 | 全部/成功/失败，默认"全部结果" |

---

## 非功能需求

### 安全性

1. **禁止越权操作：** 只读用户不得执行账号创建、停用等写操作；运营人员不得操作系统用户管理
2. **超级管理员保护：** 超级管理员账号状态不可被其他角色切换，密码不可被其他角色重置
3. **操作日志不可篡改：** 日志为只写记录，前端仅展示，不提供删除或修改功能

### 交互规范

1. **实时校验：** 所有表单输入框应在输入完成时触发实时校验，使用绿色边框+✓图标表示通过，红色边框+✗图标表示错误
2. **加载状态：** 提交按钮在执行操作时展示 loading 状态（旋转图标+文字），防止重复提交
3. **空状态：** 列表无数据时展示友好的空状态（图标+文字说明）
4. **操作反馈：** 所有增删改操作完成后通过 Toast 通知反馈结果，Toast 自动消失时间为 3 秒

### 兼容性与性能

1. 支持 Chrome 90+、Safari 14+、Edge 90+ 浏览器
2. 列表页面响应时间 ≤ 2 秒（本阶段为前端 mock，无实际网络请求）
3. 分页加载，单页最多 100 条数据，避免一次性渲染过多 DOM

---

## 数据字典

### 系统用户（SysUser）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | string | 唯一标识 |
| username | string | 登录账号 |
| realName | string | 真实姓名 |
| phone | string | 手机号 |
| email | string | 邮箱（选填） |
| department | string | 部门（选填） |
| role | SysRole | 角色枚举 |
| status | 'active' \| 'inactive' | 账号状态 |
| lastLogin | string | 最近登录时间 |
| createTime | string | 创建时间 |

### 角色枚举（SysRole）

| 枚举值 | 说明 |
|--------|------|
| super_admin | 超级管理员 |
| admin | 系统管理员 |
| operator | 运营人员 |
| viewer | 只读用户 |

### 经销商账号（Account）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | string | 唯一标识（格式：U年月日序号，如 U20260414001） |
| phone | string | 登录手机号 |
| realName | string | 用户姓名 |
| accountType | AccountType | 账号类型枚举 |
| groupName | string | 所属集团名称 |
| storeName | string | 所属门店名称（门店账号专用） |
| status | AccountStatus | 账号状态 |
| createTime | string | 创建时间 |
| lastLogin | string | 最近登录时间 |

### 账号类型枚举（AccountType）

| 枚举值 | 说明 |
|--------|------|
| system_admin | 系统管理员 |
| group | 集团账号 |
| store | 门店账号 |

### 账号状态枚举（AccountStatus）

| 枚举值 | 说明 |
|--------|------|
| active | 正常/启用 |
| inactive | 停用 |

### 操作日志（SysLog）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | number | 日志序号 |
| user | string | 操作人姓名 |
| action | string | 操作行为描述 |
| module | string | 操作模块名称 |
| target | string | 操作对象标识 |
| ip | string | 操作来源 IP |
| time | string | 操作时间 |
| result | 'success' \| 'failed' | 操作结果 |

---

## 验收标准

### F001 系统用户列表

- [ ] 列表正常展示，包含所有10列字段
- [ ] 4个角色统计卡片数字准确
- [ ] 按姓名/用户名/手机号搜索，结果实时过滤，不区分大小写
- [ ] 按角色筛选有效，"全部"时显示所有用户
- [ ] 超级管理员的状态开关为禁用态，无法点击

### F002 新建系统用户

- [ ] 点击「新建用户」弹窗正常打开
- [ ] 登录账号、真实姓名、手机号均为空时「创建」按钮禁用
- [ ] 表单填写完成后点击「创建」弹窗关闭，列表刷新
- [ ] 角色选项中不包含"超级管理员"

### F003 用户详情查看

- [ ] 点击「详情」弹窗正常打开，8个字段均正确展示
- [ ] 从未登录的用户"最近登录"显示"从未"
- [ ] 弹窗点击关闭按钮正常关闭

### F004 用户状态启用/禁用

- [ ] 点击 Switch 立即切换，状态文字联动变化
- [ ] 超级管理员状态不可切换
- [ ] 禁用/启用状态的颜色符合规范（绿色/灰色）

### F005 重置用户密码

- [ ] 点击「重置」触发 Toast 提示，提示内包含新密码
- [ ] Toast 提供「复制」操作按钮，点击后成功复制密码

### F006 经销商账号列表

- [ ] 列表展示10列字段，数据正确
- [ ] 手机号、姓名、账号类型、所属集团筛选均有效
- [ ] 分页控件正常运作，每页条数切换有效
- [ ] 状态统计（正常 N / 停用 N）计数准确
- [ ] 无数据时展示友好空状态提示

### F007 新增经销商账号

- [ ] 选择账号类型"系统管理员"：集团/门店字段隐藏
- [ ] 选择账号类型"集团账号"：仅集团字段显示
- [ ] 选择账号类型"门店账号"：集团和门店字段均显示，门店列表依赖集团选择
- [ ] 手机号输入满6位时密码自动填充为后6位
- [ ] 切换集团时门店自动清空
- [ ] 必填项为空提交时 Toast 提示错误数量
- [ ] 创建成功后 Toast 提示，弹窗关闭

### F008 账号启用与停用

- [ ] 点击停用按钮弹出确认弹窗，弹窗含橙色警告提示
- [ ] 点击启用按钮弹出确认弹窗，弹窗含绿色提示
- [ ] 确认操作后列表状态即时更新
- [ ] Toast 提示操作结果

### F009 重置账号密码

- [ ] 点击重置按钮后 Toast 提示新密码（手机号后6位）
- [ ] Toast 中「复制」按钮点击后成功复制密码到剪贴板
- [ ] 二次 Toast 提示"已复制到剪贴板"

### F010 操作日志列表

- [ ] 列表展示7列字段，数据正确
- [ ] 成功/失败标签颜色区分正确（绿/红）
- [ ] 底部"共 N 条记录"计数准确

### F011 日志筛选与查询

- [ ] 搜索框支持操作人/模块关键词搜索，结果实时过滤
- [ ] 结果筛选（全部/成功/失败）有效

---

## 相关依赖与风险

### 接口依赖

| 接口 | 状态 | 说明 |
|------|------|------|
| 系统用户 CRUD 接口 | 未接入 | 当前使用前端 Mock 数据，需后端提供 |
| 经销商账号 CRUD 接口 | 未接入 | 当前使用前端 Mock 数据，需后端提供 |
| 操作日志查询接口 | 未接入 | 当前使用静态 Mock 数据，需后端提供 |
| 密码重置接口 | 未接入 | 重置密码操作需后端实现密码加密存储 |

### 技术风险

1. **密码安全**：当前阶段密码明文存储于 localStorage，正式接入后必须使用 bcrypt 等算法加密存储，传输时使用 HTTPS
2. **权限控制**：当前前端未实现完整的路由级权限控制，正式接入后需要根据 SysRole 动态生成可访问路由和可操作按钮
3. **日志完整性**：操作日志在当前阶段为静态 Mock，正式接入后需确保服务端写入，避免前端操作绕过日志记录

### 产品风险

1. 超级管理员只有1个且不可禁用，若密码遗忘需通过数据库直接重置，需提供紧急运维方案
2. 重置密码后系统无主动通知机制，依赖管理员人工传递密码，建议后续增加短信通知功能

---

*文档结束 · PRD-GL-004 · V1.0 · 2026-04-16*
